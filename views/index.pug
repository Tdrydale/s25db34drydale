extends layout

block content
  h1= title
  p Welcome to Assignment 10
  button(type="button", class="btn btn-secondary", onClick="faster()") Button
  div(id="faster") add in extra

  script.
    var value = 1;
    var amt = 2;
    function faster() {
      amt = amt * 2;
      value = value + amt;
      document.getElementById("faster").innerText = "faster " + value + " times " + amt;
    }

  p

  a(href="/dragons") Dragons
